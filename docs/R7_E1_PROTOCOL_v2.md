# Revised R7-E1 Protocol v2 — pending R6-C review

This protocol is a frozen future-entry contract, not execution authorization. It binds revised R7-E1 to the R6-C v2 Formal-ready data, revised E1 nine-case matrix, and revised identities. The old `experiments/r7_e1/` output remains pre-revision historical diagnostic evidence and must never be used as the output directory for revised E1.

After R6-C independent review passes and the user explicitly authorizes revised R7-E1, execute exactly M0/M1/M2 at `.90/1.00/1.10 B_ref`. M1 permits only R0/basic eta=.20 with zero premium; M2 permits R0/R1/R2. Use the existing unified budget, T-COST, solver/certificate/tolerance policy and EF→A0→A1_full certification chain. Do not add timing repetitions or E2–E5 cases.

Revised outputs must use a new directory and identity chain. They must report T-COST, Q, F, selected R, x, u, budget usage, robust-cost worst scenario, fulfillable/available F, and actually exercised F. Activation is not an acceptance gate.

Current gate: `NOT_AUTHORIZED / R6-C INDEPENDENT REVIEW PENDING / FORMAL RUNS 0`.
