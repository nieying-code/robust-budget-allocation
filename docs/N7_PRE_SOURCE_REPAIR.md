# N7-pre bounded source-entry repair

N7-PRE DIAGNOSTIC ONLY — NOT FORMAL SCIENTIFIC PARAMETERS

The user independently confirmed the entry blocker and authorized bounded repair
plus conditional automatic relaunch. N1 correctly rejected ignored/untracked
files within the roots supplied by the caller. The new caller erroneously used
all `src/`, including sibling editable-install metadata. The production repair is
one line in mechanism_confirmation/audit.py: use `src/robust_budget_allocation/`
alongside unchanged `tests/`, `scripts/`, `configs/` protection. No metadata is
deleted or special-cased. Required tracked inputs, clean source checks, canonical
serialization, commit/tree object proofs, inventory hashes and replay remain.

Production-entry integration tests use actual depth-one clones and subprocesses
importing the committed production package. No monkeypatch or full-history
dependence. They cover sibling packaging metadata, ignored AND untracked inputs
in all four protected directories, required missing files, tracked tampering,
and persisted JSON with the real production gate validator. A synthetic gate
fixture tests serialization only; it is explicitly NOT licensed-test evidence.

Before any licensed/scientific execution, run full solver-free regressions and
delivery/source/replay/frozen-registration checks. Commit this implementation
before clone-based tests. Actual prelaunch uses a clean commit, real licensed
tests and preflight; only all-PASS allows Layer 1. Any new issue outside this
boundary voids automatic relaunch authorization and stops work. No protocol,
matrix, seeds, models, algorithms, thresholds or N0–N6 evidence are changed.

The prior failure JSON and source object archive remain immutable historical
evidence of zero runs at that attempt. They are not overwritten or reinterpreted.
Current scientific state will be reported separately after the authorized gates.
Until then there are no new Option/Memory observations. PR #9 remains Draft;
no automatic merge or formal N7/N8 execution.
